document.addEventListener("DOMContentLoaded",()=>{const slides=[...document.querySelectorAll(".hero-slide")],dots=document.getElementById("sliderDots");if(!slides.length)return;let current=0;slides.forEach((_,i)=>{const b=document.createElement("button");b.className=i===0?"active":"";b.addEventListener("click",()=>go(i));dots.appendChild(b)});function go(i){current=(i+slides.length)%slides.length;slides.forEach((s,n)=>s.classList.toggle("active",n===current));[...dots.children].forEach((d,n)=>d.classList.toggle("active",n===current))}document.getElementById("prevSlide")?.addEventListener("click",()=>go(current-1));document.getElementById("nextSlide")?.addEventListener("click",()=>go(current+1));setInterval(()=>go(current+1),6000)})